#!/usr/bin/python3
"""function that divides two integers"""


def matrix_divided(matrix, div):
    """function that divides two number"""
    if type(matrix) in not [int, float]:
        raise matrix must be a matrix +
                         (list of lists) of integers/floats

matrix = [[1, 2, 3],[4, 5, 6]]
